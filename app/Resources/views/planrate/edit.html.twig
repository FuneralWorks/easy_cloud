{% extends "Admin/card_container.html.twig" %}


{% block card_header %}
    <h1>Planrate modification</h1>
{% endblock card_header %}


{% block card_body %}
    {{ form_start(edit_form) }}
        <div class="form-group row"> 
            <div class="col-sm-2">
                {{ form_label(edit_form.product)}}
            </div>
            <div class="col-sm-9">
                {{ form_widget(edit_form.product) }}
            </div>
        </div>
        <div class="form-group row"> 
            <div class="col-sm-2">
                {{ form_label(edit_form.monthDuration)}}
            </div>
            <div class="col-sm-9">
                {{ form_widget(edit_form.monthDuration) }}
            </div>
        </div>
         

{% endblock card_body %}

{% block card_footer %}
    <a href="{{ path('planrate_index') }}">Retour à la liste</a>
        <input class="btn btn-success" type="submit" value="Editer" />
    {{ form_end(edit_form) }}

   

    {{ form_start(delete_form) }}
        <input class="btn btn-danger" type="submit" value="Supprimer">
    {{ form_end(delete_form) }}
{% endblock card_footer %}
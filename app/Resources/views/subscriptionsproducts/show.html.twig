{% extends 'base.html.twig' %}

{% block body %}
    <h1>Subscriptionsproduct</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subscriptionsProduct.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ subscriptionsProduct.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('subscriptionsproducts_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('subscriptionsproducts_edit', { 'id': subscriptionsProduct.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
